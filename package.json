{
  "name": "livenova",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "apps/desktop-app/frontend"
  ],
  "scripts": {
    "lint": "eslint .",
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "prettier": "^3.0.0",
    "typescript": "^5.2.2"
  }
}
